import { component$, useClientEffect$ } from "@builder.io/qwik";

export default component$(() => {
	// fetch items from smart contract
	//


	// check for account info
	useClientEffect$(() => {
		
	});
	return(<div>browse</div>);

});